services:
  # Backend API Service (Node.js)
  - type: web
    name: solarihub-backend
    runtime: node
    buildCommand: ./scripts/deploy-render-simple.sh
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000
      - key: FRONTEND_URL
        value: https://solarihub-frontend.vercel.app
    autoDeploy: true 